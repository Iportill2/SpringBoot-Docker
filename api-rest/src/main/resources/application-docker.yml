spring:
  datasource:
    url: jdbc:mysql://mysql:3306/aplicacion?createDatabaseIfNotExist=true
    username: api_user
    password: api_password
    driver-class-name: com.mysql.cj.jdbc.Driver

management:
  endpoints:
    web:
      exposure:
        include: health