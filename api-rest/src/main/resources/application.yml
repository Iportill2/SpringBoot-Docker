spring:
  config:
    import: optional:file:./.env[.properties],optional:file:../.env[.properties]
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  tomcat:
    accesslog:
      enabled: true
      directory: /dev
      prefix: stdout
      buffered: false
      suffix: ""
      file-date-format: ""
      pattern: "%h %l %u %t \"%r\" %s %b"

jwt:
  secret: ${JWT_SECRET:cambia_esta_clave_secreta_por_una_larga_de_al_menos_32_bytes_para_hs256}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
