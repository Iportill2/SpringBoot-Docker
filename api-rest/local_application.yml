spring:
  datasource:
    url: jdbc:mysql://localhost:3307/aplicacion?createDatabaseIfNotExist=true
    username: api_user
    password: api_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080